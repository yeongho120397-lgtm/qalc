import type { CategoryId } from '../types'

/** Category ids only — labels come from locale files. */
export const CATEGORIES: { id: CategoryId }[] = [
  { id: 'currency' },
  { id: 'length' },
  { id: 'weight' },
]
